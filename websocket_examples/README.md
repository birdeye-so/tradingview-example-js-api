1. Update CHAIN AND TOKEN in config.js

    ```
    {
        TOKEN: "<api key>",
        CHAIN: "<chain>"
    }
    ```

2. Run example: 

    ```
       Example: node simple/subscribe_price_token.js
    ```